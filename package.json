{
  "name": "Ads",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-mongo": "^5.0.0",
    "cors": "^2.8.5",
    "express": "4.17.1",
    "express-session": "^1.17.3",
    "mongoose": "^7.2.2",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^2.0.22"
  },
  "scripts": {
    "start": "yarn start npm-run-all -l -p start:*",
    "start:server": "nodemon server.js",
    "start:client": "cd client && yarn start",
    "build": "cd client && yarn install && yarn build",
    "postinstall": "cd client && yarn install"
  }
}
