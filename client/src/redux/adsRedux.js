//selectors

// actions
const createActionName = actionName => `app/ads/${actionName}`;

// action creators
const adsRedux = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  };
};
export default adsRedux;