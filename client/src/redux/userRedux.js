//selectors

// actions
const createActionName = actionName => `app/user/${actionName}`;

// action creators
const userRedux = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  };
};
export default userRedux;